Named arguments improves code readibility.
Named & default arguments also work for constructors.

If you use an object as the default argument, a new instance of that object is created for each invocation.